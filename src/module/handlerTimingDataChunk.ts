import { DriverRepository } from "../infra/db/repositories/driver.repository.js";

export function handlerTimingDataChunk(repo: DriverRepository) {
  return async (data: ) => {
    //...save to database
  }
}
